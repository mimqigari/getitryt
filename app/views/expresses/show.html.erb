

<div class="jumbotron">
  <h1 class="display-4">  <%= @express.full_name %> </h1>
  <p class="lead">Delivered to: <%= @express.delivery_address %>  <%= @express.package_size %> </p>
  <hr class="my-4">
  <p><%= @express.package_size %></p>
  <%= link_to 'Edit', edit_express_path(@express), class:"btn btn-secondary btn-lg" %> |
<%= link_to 'Back', expresses_path, class:"btn btn-dark btn-lg" %>

</div>
